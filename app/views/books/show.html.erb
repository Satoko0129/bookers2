<main>
<%= flash[:notice] %>
<div class="container">
  <div class="row">
    <div class="col-md-3">
  <%= render 'users/list', user: @user %>
  <%= render 'books/list', book: @newbook %>
    </div>
<div class="col-md-7 offset-md-1">
  <h2>Book detail</h2>
  <% @book %>
        <table>
          <tbody>
            <tr>
              <td>
                <%= image_tag @user.get_profile_image(100,100) %>
                <%= link_to user_path(@user) do %>
                <div><%= @user.name %></div>
                <% end %>
              </td>
              <td>
                <%= link_to book_path(@book.id) do %>
                <%= @book.title %>
                <% end %>
              </td>
              <td>
                <%= @book.body %>
              </td>
              <td>
                <% if @book.user == current_user %>
                <%= link_to "Edit", edit_book_path(@book), class: "btn btn-sm btn-success" %>
                <% end %>
              </td>
              <td>
                <% if @book.user == current_user %>
                <%= link_to "Destroy", book_path(@book), class: "btn btn-sm btn-danger",　method: :delete, date: {confirm: "are you sure?"} %>
                <% end %>
                </td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</main>